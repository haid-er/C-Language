#include <stdio.h>
#include <conio.h>
main()
{
       int n,z;
       clrscr();
       for(n=1;n<=5;n++)
       {
	 printf("Enter any no.:");
	 scanf("%d",&z);
	 if(z<=0)
	     break;
	 printf("You Enter %d\n",z);
       }
       getch();
}