#include <stdio.h>
#include <conio.h>
main()
{
    int n,b=1,c;
    clrscr();
    printf("Enter any no.:");
    scanf("%d",&n);
    for(c=1;c<=n;c++)
    {
      b=b*c;
    }
    printf("Factorial=%d",b);
    getch();
}