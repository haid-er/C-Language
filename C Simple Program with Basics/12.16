#include <stdio.h>
#include <conio.h>
main()
{
  int n,c;
  clrscr();
  printf("Enter starting no.");
  scanf("%d",&n);
  printf("Enter ending no.");
  scanf("%d",&c);
  do
  {
      if(!(n%2==0))
      {
	printf("%d\n",n);
	}
      n++;
  }while(n<=c);
  getch();
}