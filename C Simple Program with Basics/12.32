#include <stdio.h>
#include <conio.h>
main()
{
    int a,s=0,p=1;
    clrscr();
    for(a=1;a<=10;a++)
    {
       if(a%2==0)
	   p=p*a;
       else
	   s=s+a;
    }
    printf("Product of E no.=%d\nS of O no.=%d",p,s);
    getch();
}