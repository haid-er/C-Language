#include <stdio.h>
#include <conio.h>
main()
{
  int n,c;
  clrscr();
  printf("Enter starting no.");
  scanf("%d",&n);
  printf("Enter ending no.");
  scanf("%d",&c);
  while(n<=c)
  {
      if(n%2==0)
      {
	printf("%d\n",n);
	}
      n++;
  }
  getch();
}